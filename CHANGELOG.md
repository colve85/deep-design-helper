# 변경 이력

## 2.2.1

붙여넣기 모드에서 3단계 응답이 자주 실패하던 문제를 고쳤습니다.

- **AI 응답 파서 보강** — 모델이 값 안에 큰따옴표를 그대로 넣거나(오개념별 처치 발문에서
  학생의 말을 인용할 때 거의 항상 발생) 줄바꿈을 날것으로 넣어도 읽어냅니다.
  기대한 모양과 반대(객체↔배열)로 와도 맞춰 줍니다.
- **프롬프트에 안내 추가** — 값 안에서는 큰따옴표 대신 홑따옴표나 낫표를 쓰도록 지시합니다.
- **오류 문구 개선** — 단계 초안은 `{ }`, 소재 제안은 `[ ]` 부분만 붙여 넣으라고 구분해 안내합니다.
- **단계 정의 키 불일치 수정** — 2.1.0 에서 `ubd` 를 `mapping` 으로 바꾸면서 화면·한글 문서·
  프롬프트 세 곳이 `undefined` 로 나오던 것을 고쳤습니다.
- `verify.js` 에 흐트러진 응답 7가지 형태와 단계 정의 키 일치 점검을 추가했습니다.

## 2.2.0

**Vercel 배포 지원.** 소스를 나누지 않고 같은 `src/*.gs` 로 Apps Script와 Vercel 양쪽에
배포할 수 있습니다.

### 더해진 것

- `api/index.js` — Vercel 서버리스 진입점. 화면 렌더와 `google.script.run` 중계를 함께 처리.
- `lib/gas-runtime.js` — Node에서 `src/*.gs` 를 그대로 실행하는 런타임.
  `Utilities`·`Session`·`PropertiesService` 를 흉내내고, 동기 호출이 불가능한
  `UrlFetchApp` 대신 Gemini 호출을 여기서 비동기로 처리합니다.
  (기존 `tools/gas-shim.js` 를 옮겨 확장)
- `vercel.json` — 라우팅과 함수 설정.
- **브라우저 키 보관** — Vercel 배포에서는 API 키가 서버에 저장되지 않고 그 브라우저에만
  남습니다. 요청마다 전달되어 중계될 뿐입니다.
- **설정의 "저장된 키 삭제" 버튼** — 두 배포 모두에서 동작합니다.
- `verify.js` 에 Node 배포 경로 점검 추가(런타임 구분, 화면 렌더, API 중계,
  Apps Script 전용 기능 차단, 키 없을 때 처리).

### 달라진 것

- `api_bootstrap` 이 `runtime`('gas' / 'vercel')을 함께 돌려주고, 화면이 이 값으로
  드라이브 저장 버튼 노출과 키 보관 방식을 정합니다.
- 설정에서 키 칸을 비워 두고 저장하면 기존 키가 **유지**됩니다(이전에는 삭제되었습니다).
  삭제는 전용 버튼으로 합니다.
- `tools/dev-server.js` 가 Vercel과 같은 코드 경로를 쓰도록 단순해졌습니다.

## 2.1.0

「2026 전북형 깊이 있는 수업·평가 · 백워드 설계」 연수 자료(1~4단계)를 반영해
설계 단계의 구성을 연수의 산출물과 맞추었습니다.

### 더해진 것

- **탐구 질문 체계** — 1단계에 핵심 탐구 질문과 별도로 **탐구(안내) 질문** 항목 추가.
  사실적 → 개념적 → 논쟁적·성찰적 순으로 5~7개를 씁니다. 18개 단원 모두에 예시 제공.
- **핵심 평가요소** — 2단계에 성취기준·성취수준에서 뽑은 평가요소 항목 추가(세 범주 표시).
- **평가 계획** — 형성평가 시점과 총괄 시점을 함께 계획하는 항목 추가.
- **분석적 루브릭 4수준** — 매우 우수(A)·우수(B)·보통(C)·향상 필요(D~E)로 전면 교체.
  최하 수준도 결여가 아니라 관찰된 수행으로 기술.
- **단원 개관** — 3단계에 발견하기 · 탐색하기 · 적용하기 · 수행 및 성찰의 차시 배분 항목 추가.
- **오개념별 처치 발문** — 학생이 실제로 하는 말과 그 말을 흔드는 교사의 한 문장을 짝으로.
- **차시표의 학습 단계 열** — 소재마다 네 단계 중 하나를 지정. 내용 요소를 54개에서
  **72개**로 늘려 모든 단원이 발견부터 수행까지를 덮도록 보강.
- **참고 자료 화면 확장** — 15차시 로드맵, 좋은 핵심 질문의 5가지 조건, 질문 3유형,
  GRASPS 6요소, 루브릭 4수준, 학습 경험 4단계와 역순 배치 질문, 수업 나눔 Glow & Grow.
- **설계 점검 항목 추가** — 평가요소 ↔ 루브릭 연결, 루브릭 4수준 누락, 학습 단계 커버리지.

### 달라진 것

- 1단계 이름을 「단원 목표와 탐구 질문」으로, `canDo` 를 「세 범주 세부 학습 목표」로 변경.
- 각 단계에 연수 차시와 교육부 4단계 모델의 대응을 함께 표기.
- 자가 점검 체크리스트를 연수 워크북의 점검 문항으로 교체·확장(단계당 5~7개).
- AI 프롬프트에 그 단계의 자가 점검 기준이 들어가고, 소재 제안 프롬프트가 역순 배치와
  학습 단계를 함께 요구하도록 개선.
- 한글 문서의 차시별 설계표에 학습 단계 열 추가.

## 2.0.0

첫 공개 버전. 1.x 에서 달라진 점은 다음과 같습니다.

### 더해진 것

- **내용 요소별 수업 소재** — 18개 단원에 54개의 소재 카드. 각 카드에 수업 소재, 여는 질문,
  도달 목표, 확인할 증거, 학생 활동, 도구가 들어 있습니다.
- **차시별 수업 설계표** — 3단계 안에서 차시를 추가·삭제·이동하며 백워드로 설계합니다.
  소재 카드를 눌러 바로 채울 수 있습니다.
- **설계 점검** — 1단계 목표가 2단계 증거에, 2단계 증거가 3단계 활동에 이어졌는지
  자동으로 살펴보고 단계별 자가 점검 문항을 함께 보여 줍니다.
- **단계별 자가 점검 체크리스트** — 각 단계 카드 안에서 체크하며, 한글 문서에도 실립니다.
- **참고 자료 화면** — 교육부 4단계 모델과 UbD의 대응표, 교육과정이 요구하는 전환,
  자주 나타나는 오해, 총론 인용.
- **AI 소재 제안** — 차시별 수업 소재를 표 형태로 제안받습니다.
- **JSON 내보내기 · 불러오기**
- **개발 도구** — `tools/dev-server.js`(브라우저에서 그대로 실행), `tools/verify.js`(배포 전 점검),
  GitHub Actions 연동.

### 달라진 것

- 0단계에 **내용 체계 세 범주 분석**과 **학생 오개념** 항목 추가.
- 1단계에 **단원명·수업 의도**(배경 → 방향 → 핵심 목표 → 경험 예고 → 기대 성장) 항목 추가.
- 2단계에 **피드백·자기평가 계획(for · as)** 항목 추가.
- 3단계에 **탐구와 연습(드릴)의 경계** 항목 추가.
- 각 단계에 UbD ↔ 교육부 4단계 모델 대응 표기.
- AI 프롬프트에 그 단계의 자가 점검 기준과 차시별 소재가 함께 들어가도록 개선.
- 단일 `Data.gs` 를 `Stages.gs` + 과목별 단원 파일로 분리.
- 한글 문서에 차시별 수업 설계, 설계 점검 결과, 자가 점검 체크리스트 추가.

## 1.0.0

- 0~3단계 설계 화면, 단원별 예시, Gemini 자동·붙여넣기 모드, 한글 문서(.hwpx) 생성.
